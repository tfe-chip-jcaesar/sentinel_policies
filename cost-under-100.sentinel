import "tfrun"
import "decimal"

raw_monthly_cost = tfrun.cost_estimate.delta_monthly_cost else "0"
delta_monthly_cost = decimal.new(raw_monthly_cost)

main = rule {
	delta_monthly_cost.less_than(100)
}
